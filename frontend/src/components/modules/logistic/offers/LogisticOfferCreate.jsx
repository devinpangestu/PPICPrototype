import React from "react";
import { withRouter } from "react-router-dom";
import OfferForm from "./LogisticOfferForm";

const OfferCreate = (props) => {
  return <OfferForm />;
};

export default withRouter(OfferCreate);
