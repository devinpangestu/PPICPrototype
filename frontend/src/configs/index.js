import * as date from "./date";
import * as path from "./path";
import * as table from "./table";
import * as form from "./form";
import * as pagesize from "./pagesize";
import * as lang from "./lang";

const configs = {
  ...date,
  ...path,
  ...table,
  ...form,
  ...pagesize,
  ...lang,
};

export default configs;
